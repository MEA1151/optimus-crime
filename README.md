# optimus-crime
to switch between the three mode 
Mode1 ---- call the function a_p() in the loop to implement the auto-parking mode
Mode2 ---- call the function avoiding_obstacles() to implement avoiding_obstacles mode (daaah )😐
Mode3 ----- controling the car manually by calling the function ()... not determined yet
